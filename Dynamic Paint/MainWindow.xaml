<Window x:Class="Dynamic_Paint.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Dynamic_Paint.ViewModels"
        mc:Ignorable="d"
        WindowState="Maximized"
        MinWidth="640" MinHeight="480"
        Title="Dynamic Paint">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="0.6*"/>
            <RowDefinition Height="9*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Grid.Column="0" Orientation="Horizontal" Background="Aqua">
            <Menu>
                <MenuItem Header="_File">
                    <MenuItem Header="_New" />
                    <MenuItem Header="_Open" />
                    <MenuItem Header="_Save" />
                    <Separator />
                    <MenuItem Header="Exit" />
                </MenuItem>
            </Menu>
        </StackPanel>

        <WrapPanel Grid.Row="1" Grid.Column="0" Orientation="Horizontal" Background="Azure">
            <ToolBar>
                <Button MaxHeight="25" MaxWidth="25" Command="{Binding DrawLineCommand}">
                    <Image Source="Icons/line.ico" />
                </Button>
                <Button MaxHeight="25" MaxWidth="25">
                    <Image Source="Icons/rectangle.png" />
                </Button>
                <Button MaxHeight="25" MaxWidth="25">
                    <Image Source="Icons/ellipse.png" />
                </Button>
            </ToolBar>
        </WrapPanel>

        <DockPanel Grid.Row="2" Grid.Column="0" Background="Beige">
            <Canvas Background="White" />
        </DockPanel>

        <StackPanel Grid.Row="3" Grid.Column="0" Orientation="Horizontal" Background="BurlyWood">
            <StatusBar>
                <Label Content="{Binding StatusBarText}" />
            </StatusBar>
        </StackPanel>
    </Grid>
</Window>
